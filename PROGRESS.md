# Progress

Append-only session log. One dated entry per session; if today already has one, add to it instead of creating a new heading.

## 2026-09-17
- What changed: Set up local Postgres + Prisma infra — Docker Compose running Postgres 16 with a named volume and env-based credentials, Prisma initialized (no models yet), connection verified working via a throwaway smoke test.
- What's next: Design the matches/games/decisions schema and run the first migration.

## 2026-09-18
- What changed: Redid the Postgres + Prisma infra setup (prior version had been reverted). Same result — Docker Compose (Postgres 16, named volume, env-based credentials), Prisma initialized with no models, `/api/db-check` smoke-test route confirmed working — but adapted for Prisma 7: datasource URL now lives in `prisma7.config.ts` (not `schema.prisma`), and the client requires the `@prisma/adapter-pg` driver adapter. Used `prisma init --no-skills` to skip the CLI's agent-skill scaffolding this time.
- What changed: Split the single `/game_reviews` page into `/matches` (paginated match list from Galaxy's `analyses/list/{page}`) and `/matches/[matchId]` (the existing PR/mistakes analysis, moved as-is). Added `GameStatsProvider`/`useGameStatsAuth()` — an in-memory-only auth context shared across both pages, deliberately not named `AuthProvider`/`useAuth` to leave room for real app auth later — and a blocking `TokenModal` that gates `/matches` until a token is entered. `analyses/list` request URL was assumed (not confirmed against a real call) to live at the same host/prefix as `game_reviews`; flagged as unverified.
- What's next: Design the matches/games/decisions schema and run the first migration. Confirm the `analyses/list/{page}` request URL against a real token/response — current implementation is an educated guess based on the `game_reviews` endpoint's pattern.
- What changed: Defined the `Match`/`Game`/`Decision` Prisma schema and applied the first migration (`init_schema`) against local Postgres — confirmed live this time, Docker Desktop turned out to already be installed and running, just not on `PATH`. Final schema: `Match.playedAt` (nullable, filled at detail ingest) plus `createdAt` (ingest timestamp) kept distinct; `mwc` promoted to its own nullable column on `Decision`; `Decision.raw` holds the complete original event object (not just `reviews[0]`) as the zero-blind-spot archive; indexes on `(kind, classification)` and `userId`. Verified table structure directly via `psql \d`.
- What's next: Build the index-sync job (match list ingest → `Match` rows) and the detail-ingest job (per-match `game_reviews` fetch → `Game`/`Decision` rows, filling in both `playedAt` fields). `analyses/list/{page}` URL still unconfirmed against a real response (see above).
- What changed: Added `prisma/seed.ts` — fake local-dev data (2 matches, one fully "detail-ingested" with 3 games × 10 decisions each, one index-only with zero games), obviously-placeholder content (`TEST Opponent Alpha/Beta`, fake country codes, tagged `raw` JSON) so it can't be mistaken for real ingest. Wired via `prisma7.config.ts`'s `migrations.seed` (not `package.json`'s `prisma.seed` — that's the pre-v7 convention and doesn't work under Prisma 7). Added `tsx` as a dev dependency to run it, since the generated client is TS-source-only. Idempotent (`deleteMany` in FK-safe order before inserting) — verified by running `npx prisma db seed` twice with identical row counts. Also fixed a real bug this surfaced: `prisma/seed.ts`'s BigInt literal broke `next build`'s type-check because the file was being swept into the app's tsconfig; excluded `prisma/seed.ts` in `tsconfig.json` since it's a standalone script, not app code.
- What's next: Build the index-sync job and the detail-ingest job (unchanged from above).
- What changed: Built the DB-backed read path, additive and parallel to the existing Galaxy-fetching code (nothing under `/matches` or `/api/matches`/`/api/game_reviews` touched). `lib/local-client.ts` (`listMatches`, `getGameReviews`) queries Prisma and returns the same `AnalysesListResponse`/`GameReviewsResponse` shapes; `getGameReviews` reconstructs a game's events purely from each `Decision.raw`. New routes `GET /api/local/matches/list/[page]` and `GET /api/local/matches/[matchId]/[gameIndex]` (404 when not ingested); new pages `/db/matches` and `/db/matches/[matchId]` (no token/modal — reads the local DB) reusing the existing `MistakesSection`/`BoardPanel`/`Board`/`Dice` components in place via import, not moved. Added `lib/prisma.ts` as a shared dev-mode PrismaClient singleton. Had to revise `prisma/seed.ts`'s `raw` field from a bare placeholder marker to a fully-shaped `GameEvent` object (valid decodable position id, real analysis envelope) — the placeholder from the prior seed task couldn't actually drive the UI, which this step's verification required. Verified end-to-end: ran `extractDecisions`/`computePR` against the live reconstructed API response — 30/30 decisions parsed correctly (18 checker/12 cube, 15 none/12 error/3 blunder, matching the seed exactly), position id decodes to a valid 15-checker board. Also hit and fixed a real Next.js routing constraint along the way: sibling dynamic folders can't use different slug names at the same level (`[page]` vs `[matchId]`), so the list route moved to `/api/local/matches/list/[page]`.
- What's next: The next separate step is the shared-interface abstraction / routes under `/galaxy/...` (explicitly deferred here), plus the sync button and the index-sync/detail-ingest jobs themselves (unchanged from above).
- What changed: Finalized routing for both data sources. Added `lib/types/data-client.ts` (`MatchDataClient` interface) and `lib/galaxy-client.ts` (`createGalaxyClient(token): MatchDataClient`, same Galaxy fetch behavior as before — same URLs, same "empty events array = end of match" signal, same "no models" honesty — just reshaped to one game per call instead of a server-side streaming loop). `local-client.ts` already conformed to the interface exactly; added an explicit `localClient: MatchDataClient` export. DB-backed routes/pages renamed to their final, un-prefixed names — `/api/matches/list/[page]`, `/api/matches/[matchId]/[gameIndex]`, `/matches`, `/matches/[matchId]` — now the default, no-token path. Galaxy-backed routes/pages moved to `/api/galaxy/matches/list/[page]`, `/api/galaxy/matches/[matchId]/[gameIndex]`, `/galaxy/matches`, `/galaxy/matches/[matchId]` (token modal moved with it, unchanged behavior). Moved the shared `Board`/`BoardPanel`/`Dice`/`MistakesSection` components to `app/components/match-analysis/` since both detail pages now need them. Landing page link was already pointed at `/matches`, which now correctly means the DB-backed default — no change needed there. Hit the same sibling-dynamic-slug Next.js constraint as the previous step on *both* new API namespaces (`[page]` vs `[matchId]` under one parent), resolved the same way (`.../list/[page]`). Verified: `/matches` renders with no token modal, `/galaxy/matches` still shows it; both API namespaces respond correctly (tested the Galaxy side with a bad token — real 401s come back cleanly, confirming the endpoint URLs are genuinely correct); re-ran the `extractDecisions`/`computePR` pipeline check against the renamed `/api/matches/*` routes — identical result (30 decisions, same PR) to before the rename.
- What's next: The sync button, plus the index-sync/detail-ingest jobs (unchanged from above) — no more deferred steps ahead of those now that routing is finalized.
